import { TransactionHistoryDto } from './transaction-history-dto';

describe('TransactionHistoryDto', () => {
  it('should create an instance', () => {
    expect(new TransactionHistoryDto()).toBeTruthy();
  });
});
